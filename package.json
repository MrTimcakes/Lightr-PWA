{
  "name": "lightr-pwa",
  "version": "0.1.0",
  "description": "The PWA for Lightr",
  "author": "MrTimcakes",
  "license": "GPL v3",
  "main": "index.js",
  "scripts": {
    "test": "eslint src && preact test",
    "start": "if-env NODE_ENV=production && npm run -s serve || npm run -s dev",
    "build": "preact build",
    "serve": "preact build && preact serve",
    "dev": "preact watch"
  },
  "keywords": [],
  "eslintConfig": {
    "extends": "eslint-config-synacor"
  },
  "devDependencies": {
    "eslint": "^4.3.0",
    "eslint-config-synacor": "^1.0.1",
    "if-env": "^1.0.0",
    "node-sass": "^4.5.3",
    "preact-cli": "^1.4.1",
    "sass-loader": "^6.0.6"
  },
  "dependencies": {
    "localforage": "^1.5.0",
    "node-sass": "^4.5.3",
    "paho-mqtt": "^1.0.3",
    "preact": "^8.2.1",
    "preact-compat": "^3.16.0",
    "preact-material-components": "^1.1.3",
    "preact-router": "^2.5.5",
    "sass-loader": "^6.0.6"
  }
}
